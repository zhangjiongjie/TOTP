# TOTP密令 HarmonyOS 上架资料草稿

更新时间：2026-06-05

## 当前项目识别信息

| 字段 | 内容 |
| --- | --- |
| 应用中文名 | TOTP密令 |
| 应用英文名 | TOTP Token |
| 包名 / Bundle Name | `com.totp.authenticator` |
| 当前版本号 | `1.0.0` |
| 当前 versionCode | `6` |
| 支持设备 | 手机、平板 |
| SDK | HarmonyOS 6.1.0(23) |
| 权限 | `ohos.permission.INTERNET`、`ohos.permission.ACCESS_BIOMETRIC` |
| 当前签名状态 | 当前 `build-profile.json5` 是调试签名，上架前需要切换为发布签名 / 上架 Profile。 |

## AGC 可填写内容

### 应用名称

TOTP密令

### 一句话简介 / 简短描述

面向 HarmonyOS NEXT 的 TOTP 双重验证码管理器，支持加密保管库、WebDAV 同步和生物识别解锁。

### 应用介绍 / 详细描述

TOTP密令是一款面向 HarmonyOS NEXT 设备的 TOTP 双重验证码管理工具，适用于搭载 HarmonyOS NEXT 操作系统的手机和平板。应用采用本地优先设计，账号密钥默认保存在本机加密保管库中，未配置 WebDAV 时不会上传到云端。

本次上架版本为 HarmonyOS NEXT 原生应用，使用 HarmonyOS 系统能力提供扫码、相册选择、文件导入导出和生物识别解锁体验，并适配 HarmonyOS NEXT 的浅色 / 深色模式与手机、平板界面。

你可以手动添加账号，也可以通过 otpauth 链接、扫码或相册二维码识别导入账号。应用支持常见的 6 位和 8 位验证码，支持 SHA1、SHA256、SHA512 算法，并提供验证码倒计时与一键复制能力。

如需在 HarmonyOS NEXT、Android 和浏览器扩展等多端之间同步数据，可以自行配置 WebDAV 服务。同步数据会以加密保管库形式保存到用户指定的 WebDAV 路径中，并支持本地与远端数据变更判断、冲突提示、远端主密码验证和加密备份导入导出。

应用还支持主密码保护和系统生物识别解锁。生物识别仅用于本机解锁便利性，用户仍可随时使用主密码解锁。

### 主要功能亮点

- 面向 HarmonyOS NEXT 手机和平板，使用原生系统能力提供解锁、扫码、相册和文件选择体验。
- 本地加密保存 TOTP 账号密钥。
- 支持主密码解锁和系统生物识别解锁。
- 支持手动录入、otpauth 链接解析、扫码和相册二维码识别。
- 支持验证码倒计时、一键复制和常见服务品牌图标。
- 支持可选 WebDAV 加密同步，不配置时完全本地使用。
- 支持加密备份导入导出，便于迁移和恢复。
- 支持浅色 / 深色模式，适配手机和平板。

### 关键词建议

双重验证, 验证码, TOTP, 2FA, MFA, 认证器, 密码管理, WebDAV, 加密备份, 生物识别

### 应用分类建议

工具 / 实用工具

### 付费类型建议

免费

### 年龄分级建议

建议选择最低年龄分级。应用不包含社交、UGC、购物、赌博、暴力、成人内容或位置追踪。最终以 AGC 年龄分级问卷为准。

### 版本更新说明

首个 HarmonyOS NEXT 上架版本，支持本地加密 TOTP 保管库、验证码展示与复制、账号添加编辑删除、WebDAV 加密同步、加密备份导入导出、主密码修改和生物识别解锁。

### 审核备注

本应用是面向 HarmonyOS NEXT 的 TOTP 双重验证码管理工具。首次启动后需要设置本机主密码，然后可通过手动输入、otpauth 链接、扫码或相册二维码识别添加账号。

WebDAV 同步为可选功能，需要用户自行填写 WebDAV 服务器地址、路径、用户名和密码；不配置 WebDAV 时，数据仅保存在本机加密保管库中。

应用申请网络权限仅用于用户主动配置的 WebDAV 同步；申请生物识别权限仅用于用户主动开启后的本机解锁。应用不包含广告、不包含内购、不采集定位信息，不向开发者服务器上传账号密钥或验证码数据。

如审核需要测试 WebDAV，同步功能可以不配置；核心本地验证码管理、导入导出和生物识别开关均可独立测试。

## 权限用途说明

| 权限 | 用途 | 是否必须 |
| --- | --- | --- |
| `ohos.permission.INTERNET` | 访问用户自行配置的 WebDAV 服务，用于加密同步和加密保管库上传 / 下载。 | WebDAV 同步需要；本地使用不需要主动联网。 |
| `ohos.permission.ACCESS_BIOMETRIC` | 调用系统生物识别能力，用于本机解锁验证。 | 用户开启生物识别解锁时需要。 |

## 数据与隐私说明摘要

- 账号发行方、账号名、TOTP 密钥、分组、算法、位数和周期保存在本机加密保管库中。
- 主密码用于保护本机保管库，不会上传到开发者服务器。
- WebDAV 同步完全由用户自行配置，远端数据以加密保管库形式保存到用户指定的 WebDAV 服务。
- 生物识别凭据使用系统能力和本机安全存储，仅用于本机解锁。
- 扫码和相册二维码识别仅用于解析用户选择或扫描的 otpauth 二维码。
- 剪贴板仅在用户点击复制验证码时写入当前验证码。

## 仍需你补充 / 确认的资料

1. 隐私政策 URL：AGC 通常要求填写线上可访问的隐私政策链接。仓库里已准备草稿，仍需发布到你的网站、GitHub Pages、Gitee Pages 或其他公开页面。
2. 开发者联系方式：客服邮箱、联系电话或反馈渠道。建议至少准备一个邮箱。
3. 发布签名与上架 Profile：当前工程使用调试签名，不能直接作为正式上架包。
4. 截图素材：建议准备手机截图至少 3 张，平板如果选择支持也建议准备平板截图。
5. 应用图标：项目已有图标资源，但 AGC 是否需要单独上传应用图标，请按页面要求上传 `AppScope/resources/base/media/app_icon.png` 或重新导出符合尺寸要求的 PNG。
6. WebDAV 测试说明：如果审核强制测试同步，最好准备一个测试 WebDAV 账号；否则在审核备注中说明 WebDAV 为可选用户自配功能。
7. 版权/商标风险确认：应用内含常见服务品牌图标。若 AGC 对第三方品牌图标敏感，建议在审核前确认图标来源和授权，或只保留通用默认图标。

## 上架前工程检查

- 将 `build-profile.json5` 中调试签名替换为发布签名。
- 使用 `release` build mode 构建并确认 HAP / APP 包可安装。
- 真机验证：首次设置主密码、添加账号、扫码/相册导入、复制验证码、锁定后重新解锁、生物识别开关、导入导出、WebDAV 不配置状态。
- 检查 AGC 隐私合规问卷中的数据项与本文件一致。
- 确认 AGC 后台应用名称、应用资质的软件名称、安装包内展示名称均为「TOTP密令」，或均调整为资质文件中的同一正式名称。
